export default function EnvTestPage() {
  return (
    <main style={{ padding: 24, fontFamily: "monospace" }}>
      <h1>Env test</h1>
      <pre>NEXT_PUBLIC_MOVCO_API_URL = {process.env.NEXT_PUBLIC_MOVCO_API_URL}</pre>
    </main>
  );
}
